import java.util.*;
public class CombinationSum {
    static List<List<Integer>> Sum(int arr[],int target){
        List<List<Integer>> ans = new ArrayList<>();
        List<Integer> res= new ArrayList<>();
        Helper(arr, target,0, ans, res);
        return ans;
    }

    static void Helper(int arr[],int target, int index,List<List<Integer>> ans , List<Integer> res){

        // Base Case
        if (index== arr.length) {
            
        }
for (int i = 0; i < arr.length; i++) {
    
}
    }
}
